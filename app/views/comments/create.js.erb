//We hide the message saying no comment if present
$(".comments h3").hide();
//Hide the comment form
$(".newComment form").hide();
//Display the new comment
$(".comments").append("<%= escape_javascript(render :partial => @comment) %>");

